---@mod processing.config.default Default processing.nvim configuration.

---@type processing.Config
local default = {
    highlight = {
        enable = true,
    },
    lsp = {
        cmd = nil,
    },
}

return default
